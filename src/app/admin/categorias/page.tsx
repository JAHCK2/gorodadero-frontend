// Admin — Gestión de categorías
// TODO: CRUD de categorías, drag & drop para reordenar, toggle activo/inactivo

export default function AdminCategoriasPage() {
    return (
        <main>
            <h1>Gestión de Categorías</h1>
            {/* TODO: CategoryList, CreateCategoryForm, SortableList */}
        </main>
    );
}
