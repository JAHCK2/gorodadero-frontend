// Admin — Gestión de productos (CRUD)
// TODO: DataTable con filtro/búsqueda, botón crear, editar inline, pausar/activar

export default function AdminProductosPage() {
    return (
        <main>
            <h1>Gestión de Productos</h1>
            {/* TODO: SearchFilter, ProductDataTable, CreateProductModal */}
        </main>
    );
}
