// Admin — Gestión de pedidos
// TODO: Lista de órdenes en tiempo real, filtrar por estado, marcar como despachado

export default function AdminPedidosPage() {
    return (
        <main>
            <h1>Gestión de Pedidos</h1>
            {/* TODO: OrderFilters, OrderList (realtime), OrderDetailModal */}
        </main>
    );
}
